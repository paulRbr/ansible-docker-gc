---
galaxy_info:
  author: Paul Bonaud
  description: "Use spotify's docker-gc script as a cron to clean old docker images"
  license: MIT
  min_ansible_version: 1.2
  platforms:
    - name: Ubuntu
      versions:
        - precise
        - raring
        - saucy
        - trusty
  categories:
    - development
    - packaging
    - system
dependencies: []
  # List your role dependencies here, one per line. Only
  # dependencies available via galaxy should be listed here.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
